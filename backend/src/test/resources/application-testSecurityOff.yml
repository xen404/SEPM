logging:
  file.name: ./log/backend-test.log
  level:
    root: DEBUG

spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=PostgreSQL
  jpa:
    hibernate:
      ddl-auto: create

security:
  enabled: false